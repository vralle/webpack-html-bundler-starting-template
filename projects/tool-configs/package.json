{
  "name": "@vralle/tool-configs",
  "version": "0.3.0",
  "description": "Shareable tool configurations",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "format": "dprint fmt",
    "lint": "eslint --fix-dry-run --cache --cache-location .cache/.eslintcache ."
  },
  "devDependencies": {
    "@types/clean-css": "^4.2.11",
    "@types/html-minifier-terser": "^7.0.2",
    "postcss-load-config": "^6.0.1"
  }
}
